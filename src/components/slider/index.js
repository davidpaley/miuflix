import React from 'react';
import './_slider.scss';

const Slider = () => (
  <section className="slide">
    <h1 className="slide-title"><strong>MIUFLIX</strong></h1>
  </section>
);

export default Slider;
